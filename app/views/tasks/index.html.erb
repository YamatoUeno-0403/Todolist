<div class="container">
    <div class="row">
        <% @tasks.each do |t| %>
            <div class="col-8">
                <%= t.title %>
                <%= t.contnt %>
            </div>
        <% end %>
    </div>
</div>